// import initStoryshots from '@storybook/addon-storyshots';
// initStoryshots({ /* configuration options */ });

describe('PlaceHolder for storybook', () => {

    test('just a basic placeholder for the storybook config', () => {
        expect(true).toBe(true);
    });
});